1.What is count frequency of Element?
==>We can use a dictionary to store each element as a Key and occurrence count as value.
2.What is List?
==> * List is a Orederd,Mutable(changable),and allow the duplicate element in a list.
    * List use the [] square bracket
3.Why List is Mutable?
==>List can be add,remove,delete we change the data during the program Execution.
4.Write a difference b/w list and tuple
      List                                      Tuple
*List are Ordered                        *Tuples are Ordered
*List are Mutable                        *Tuples are Immutable
*List can allow Duplicate                *Tuples can allow Duplicate
*List are [] square Braket               *Tuples are () parantheses
*E.g:[1,2,34,5]                          *E.g:[3,45,6,7]
4.Write the Difference b/w append() and extend()
==>    append()                                    extend()
*Add one Element                           *Add multiple Element
*Accepts any Object                        *Accepts an iterable(list,tuple,string
*Add the object as a single item           *Add each item individually
*List length increases by 1                *List length increase by the number of added elements
