1.What is Tuples?
==>Tuples are the collection item,just like a list once it created we cannot change it.
2.Why tuples are Immutable?
==>Once the tuples are create we cannot change it.
3.Difference b/w list and Tuple.
==>List
Uses []
Mutable
Can add, remove, and update elements

Tuple
Uses ()
Immutable
Cannot add, remove, or update elements
4.Can tuples contains mutable object.
==>Yes,Tuples contains mutable object like list,set,dictonary.
E.g: t=(1,[2,3],5)  #1 is index 0,[2,3] in index 1,5 in index 2
t[1].append(4)
print(t)
5.Why tuples are faster than the list?
==>Tuples are faster than the list because of they are immutable.we cannot change or remove,delete,python stores more effiencently,less memory,they can acces the element faster then the list.

